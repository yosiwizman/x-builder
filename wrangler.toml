#:schema node_modules/wrangler/config-schema.json
name = "x-builder"
compatibility_flags = ["nodejs_compat"]
compatibility_date = "2024-07-01"
pages_build_output_dir = "./build/client"

# NOTE: Cloudflare Pages cannot bind R2 buckets via the dashboard UI.
# R2 access for the r2_worker provider happens exclusively via HTTP:
#   Pages (API) -> HTTP -> R2 Worker -> R2 Bucket
# See workers/r2-sites/ for the R2 Worker implementation.
