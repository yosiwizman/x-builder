#:schema node_modules/wrangler/config-schema.json
name = "x-builder"
compatibility_flags = ["nodejs_compat"]
compatibility_date = "2024-07-01"
pages_build_output_dir = "./build/client"

# NOTE: R2 bucket binding for R2 publish provider must be configured
# via Cloudflare Pages dashboard (Settings > Functions > R2 bucket bindings):
#   Binding name: SITES_BUCKET
#   Bucket: x-builder-sites (create with: wrangler r2 bucket create x-builder-sites)
